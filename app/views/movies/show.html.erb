<% content_for :title, @movie.title %>
<div class="container">
  <div class="row">
    <div class="col-xl-12">
      <div class="row">
        <div class="col-xl-6">
          <%= render @movie %>
          <%= render 'movie_credits', movie: @movie %>
        </div>
        <div class="col-xl-1"></div>
        <div class="col-xl-5 mt-3">
          <%= render 'movie_schedules', movie_schedules: @movie_schedules %>
        </div>
      </div>
      <div class="row mt-3">
      </div>
    </div>
  </div>
</div>
